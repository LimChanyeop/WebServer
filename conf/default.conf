user                    nobody;
worker_processes        auto;

http {
    client_limit_body_size      40000;
    request_limit_header_size   100000;

    server {
        listen                  127.0.0.1; #default 80 port

        server_name             clim.com;
        root                    /Users/limchanyeop/WebServer/;
        index                   index.html index.php;

        location                / {
            index                   index.html index.php;
            autoindex               on;
        }

        location                /myPathPath {
            return 301              /redirectionPath;
        }

    }

    server {
        listen                  4242;
        autoindex               on;

        server_name             minsikim.com;
        root                    /Users/minsikim/WebServer;
        index                   index.html;
        error_page              403 403.html;
        cgi_path                ./cgiBinary/php-cgi;
        limit_except            DELETE;

        location                / {
            index                   index.html;
            autoindex               off;
        }
        location                /redi {
            index                   index.html;
            autoindex               on;
        }
        location                /limit_except {
            limit_except            GET POST;
            autoindex               on; #a bb d; dsad 
        }

        # location                .py$ {
        #     cgi_path                /usr/bin/python3;
        # } //bonus

    }

    }
}